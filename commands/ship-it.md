---
description: "Commit, branch, push, PR, and squash-merge the current work in one step. Optional argument overrides the branch name: /ship-it fix/broken-export"
disable-model-invocation: true
---

Invoke the hartye-skills:ship-it skill and follow it exactly as presented to you.

If an argument was provided, use it as the branch name instead of deriving one from the diff.
